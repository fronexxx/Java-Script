// Task-1
let square = (a, b) => (a * b);
console.log(square(10, 5));

// Task-2
let roundSquare = (radius) => (Math.PI * radius * radius);
console.log(roundSquare(5));

// Task-3
let squareCilindre = (radius, height) => (2 * Math.PI * radius * height);
console.log(squareCilindre(5, 10));

